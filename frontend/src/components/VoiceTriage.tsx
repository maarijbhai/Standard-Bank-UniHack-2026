// VoiceTriage — core POC component (to be implemented)
// Handles: Hold-to-Speak → STT → POST /triage → play Polly audio response

export default function VoiceTriage() {
  return (
    <div>
      {/* Hold to Speak button will live here */}
    </div>
  );
}
